import { ImageSlide } from "@/components/imageSlide";

const HomePage = () => {
  return (
    <main>
      <section>
        <ImageSlide />
      </section>
    </main>
  );
};

export default HomePage;
